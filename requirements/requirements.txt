cython
plyfile

ujson  # make json loading in cocoapi_nvidia faster
pycocotools

cffi
ninja
black
docformatter
setproctitle
# meshplex  # all versions require py>=3.9 (env is 3.8) -> aborts the whole install.
#           only used by lib/render_vispy (vispy renderer); inference uses egl_renderer.
OpenEXR
vispy>=0.6.4
yacs>=0.1.8
tabulate
pytest-runner
pytest
ipdb
tqdm
numba
opencv-python
# mmcv-full  # installed via openmmlab cu116/torch1.13 wheel in the Dockerfile
imagecorruptions
pyassimp==4.1.3  # 4.1.4 will cause egl_renderer SegmentFault
pypng
imgaug>=0.4.0
albumentations==1.3.1  # last line supporting py3.8; unpinned needs py>=3.9
transforms3d
# pyquaternion
# torchvision  # provided by conda (pinned 0.14.1); pip would upgrade torch to CPU 2.4.1
open3d
fvcore
tensorboardX
einops
# pytorch3d  # installed via prebuilt py38_cu116_pyt1131 wheel in the Dockerfile
timm==0.6.7
glfw
imageio
imageio-ffmpeg
PyOpenGL  # >=3.1.5
PyOpenGL_accelerate
chardet

thop  # https://github.com/Lyken17/pytorch-OpCounter
loguru


pytorch-lightning==1.7.7  # repo uses the old LightningLite API (pytorch_lightning.lite.wrappers._LiteModule); removed in 1.9. 1.7.7 still ships it. unpinned pulls 2.x and upgrades torch to CPU.
fairscale
# deepspeed  # not imported anywhere (only a CLI help string); latest needs py>=3.9

# verified versions
# The following are export-only and NOT imported anywhere in core/lib (inference
# path). Their ancient pins deadlock the resolver against pytorch-lightning/
# tensorboard protobuf constraints, aborting the whole install. Dropped.
# pyro-ppl==1.4.0 #for epro-pnp <1.8.1
# onnx==1.8.1
# onnxruntime==1.8.0
# onnx-simplifier==0.3.5
